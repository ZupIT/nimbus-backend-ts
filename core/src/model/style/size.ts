export interface Size {
  width?: number,
  height?: number,
  minWidth?: number,
  minHeight?: number,
  maxWidth?: number,
  maxHeight?: number,
  /**
   *  @default false
   */
  clipped?: boolean,
}
